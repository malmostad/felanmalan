{
  "name": "list-of-ingredients",
  "license": "MIT",
  "engines": {
    "node": "10.12.0",
    "yarn": "1.12.3"
  },
  "scripts": {
    "build": "yarn --cwd client install --check-files && yarn --cwd client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}
